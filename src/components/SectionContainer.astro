---
interface Props {
    id?: string;
    class?: string;
}

const { class: className = "", id } = Astro.props;
---

<section id={id} data-section={id} class={`scroll-mt-24 ${className}`}>
    <div class="w-full px-6 mx-auto max-w-6xl md:px-12">
        <slot />
    </div>
</section>
